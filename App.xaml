<Application x:Class="ExplorerPro.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ExplorerPro">
    <!-- Remove StartupUri attribute - we create MainWindow programmatically in OnStartup -->
    <Application.Resources>
        <!-- Application resources use merged dictionaries for theme support -->
        <ResourceDictionary>
            <!-- The ThemeManager will replace these dictionaries at runtime -->
            <ResourceDictionary.MergedDictionaries>
                <!-- BaseTheme.xaml and the theme-specific xaml will be loaded by ThemeManager -->
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Any global non-theme specific resources can be defined here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>