<UserControl x:Class="ExplorerPro.UI.FileTree.ImprovedFileTreeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExplorerPro.UI.FileTree"
             xmlns:behaviors="clr-namespace:ExplorerPro.UI.FileTree.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/FileTreeResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converter for level to margin -->
            <local:LevelToIndentConverter x:Key="LevelToIndentConverter"/>
            
            <!-- Converter for expander positioning to the right of lines -->
            <local:ExpanderRightIndentConverter x:Key="ExpanderRightIndentConverter"/>
            
            <!-- Boolean to Visibility Converter -->
            <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            
            <!-- Multi-select converters -->
            <local:BoolToMultiSelectTagConverter x:Key="BoolToMultiSelectTagConverter"/>
            <local:MultiSelectVisibilityConverter x:Key="MultiSelectVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <!-- Main grid WITHOUT shared size scope for better performance -->
    <Grid x:Name="MainGrid" Background="{DynamicResource BackgroundColor}">
        <!-- Column Headers Row -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Column Headers with Border for background -->
        <Border Grid.Row="0" Background="{DynamicResource BackgroundColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <!-- Header ScrollViewer for horizontal sync -->
            <ScrollViewer x:Name="HeaderScrollViewer" 
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Disabled"
                          CanContentScroll="False">
                <Grid x:Name="HeaderGrid" Height="28">
                    <Grid.ColumnDefinitions>
                        <!-- Checkbox column for multi-selection -->
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition x:Name="NameColumn" Width="250" MinWidth="100" MaxWidth="600"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition x:Name="SizeColumn" Width="100" MinWidth="60" MaxWidth="150"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition x:Name="TypeColumn" Width="120" MinWidth="80" MaxWidth="200"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition x:Name="DateColumn" Width="150" MinWidth="100" MaxWidth="250"/>
                        <ColumnDefinition Width="*"/> <!-- Fill remaining space -->
                    </Grid.ColumnDefinitions>
                    
                    <!-- Multi-select checkbox header -->
                    <CheckBox Grid.Column="0" 
                              x:Name="SelectAllCheckBox"
                              Margin="4,0,4,0"
                              VerticalAlignment="Center"
                              Visibility="{Binding IsMultiSelectMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                              Click="SelectAllCheckBox_Click"
                              ToolTip="Select/Deselect All"/>
                    
                    <!-- Column Headers with hover effects -->
                    <Border Grid.Column="1" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0" 
                            Background="Transparent">
                        <Grid>
                            <TextBlock Text="Name" Padding="8,4" VerticalAlignment="Center" 
                                     Foreground="{DynamicResource TextColor}" FontWeight="SemiBold"/>
                            <!-- Sort indicator -->
                            <Path x:Name="NameSortIndicator" HorizontalAlignment="Right" VerticalAlignment="Center"
                                  Margin="0,0,8,0" Width="8" Height="8" Visibility="Collapsed"
                                  Data="M 0 4 L 4 0 L 8 4 Z" Fill="{DynamicResource TextColor}"/>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#10000000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    
                    <!-- Enhanced GridSplitter with smooth resize behavior -->
                    <GridSplitter Grid.Column="2" behaviors:ColumnResizeBehavior.EnableSmoothResize="True" Tag="Name">
                        <GridSplitter.Style>
                            <Style TargetType="GridSplitter">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Cursor" Value="SizeWE"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Margin" Value="-2,0,-2,0"/>
                                <Setter Property="Panel.ZIndex" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridSplitter">
                                            <Grid Background="{TemplateBinding Background}">
                                                <!-- Invisible hit area for easier grabbing -->
                                                <Rectangle Fill="Transparent" Width="9"/>
                                                <!-- Visual indicator -->
                                                <Rectangle x:Name="Indicator" 
                                                         Width="1" 
                                                         HorizontalAlignment="Center"
                                                         Fill="{DynamicResource BorderColor}"
                                                         Opacity="0"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0.5" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="3" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="1" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GridSplitter.Style>
                    </GridSplitter>
                    
                    <Border Grid.Column="3" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0" 
                            Background="Transparent">
                        <Grid>
                            <TextBlock Text="Size" Padding="8,4" VerticalAlignment="Center" TextAlignment="Right" 
                                     Foreground="{DynamicResource TextColor}" FontWeight="SemiBold"/>
                            <Path x:Name="SizeSortIndicator" HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Margin="8,0,0,0" Width="8" Height="8" Visibility="Collapsed"
                                  Data="M 0 4 L 4 0 L 8 4 Z" Fill="{DynamicResource TextColor}"/>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#10000000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    
                    <GridSplitter Grid.Column="4" behaviors:ColumnResizeBehavior.EnableSmoothResize="True" Tag="Size">
                        <GridSplitter.Style>
                            <Style TargetType="GridSplitter">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Cursor" Value="SizeWE"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Margin" Value="-2,0,-2,0"/>
                                <Setter Property="Panel.ZIndex" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridSplitter">
                                            <Grid Background="{TemplateBinding Background}">
                                                <Rectangle Fill="Transparent" Width="9"/>
                                                <Rectangle x:Name="Indicator" 
                                                         Width="1" 
                                                         HorizontalAlignment="Center"
                                                         Fill="{DynamicResource BorderColor}"
                                                         Opacity="0"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0.5" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="3" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="1" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GridSplitter.Style>
                    </GridSplitter>
                    
                    <Border Grid.Column="5" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0" 
                            Background="Transparent">
                        <Grid>
                            <TextBlock Text="Type" Padding="8,4" VerticalAlignment="Center" 
                                     Foreground="{DynamicResource TextColor}" FontWeight="SemiBold"/>
                            <Path x:Name="TypeSortIndicator" HorizontalAlignment="Right" VerticalAlignment="Center"
                                  Margin="0,0,8,0" Width="8" Height="8" Visibility="Collapsed"
                                  Data="M 0 4 L 4 0 L 8 4 Z" Fill="{DynamicResource TextColor}"/>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#10000000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    
                    <GridSplitter Grid.Column="6" behaviors:ColumnResizeBehavior.EnableSmoothResize="True" Tag="Type">
                        <GridSplitter.Style>
                            <Style TargetType="GridSplitter">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Cursor" Value="SizeWE"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Margin" Value="-2,0,-2,0"/>
                                <Setter Property="Panel.ZIndex" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridSplitter">
                                            <Grid Background="{TemplateBinding Background}">
                                                <Rectangle Fill="Transparent" Width="9"/>
                                                <Rectangle x:Name="Indicator" 
                                                         Width="1" 
                                                         HorizontalAlignment="Center"
                                                         Fill="{DynamicResource BorderColor}"
                                                         Opacity="0"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0.5" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="3" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Opacity"
                                                                               To="0" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="Indicator"
                                                                               Storyboard.TargetProperty="Width"
                                                                               To="1" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GridSplitter.Style>
                    </GridSplitter>
                    
                    <Border Grid.Column="7" Background="Transparent">
                        <Grid>
                            <TextBlock Text="Date Modified" Padding="8,4" VerticalAlignment="Center" 
                                     Foreground="{DynamicResource TextColor}" FontWeight="SemiBold"/>
                            <Path x:Name="DateSortIndicator" HorizontalAlignment="Right" VerticalAlignment="Center"
                                  Margin="0,0,8,0" Width="8" Height="8" Visibility="Collapsed"
                                  Data="M 0 4 L 4 0 L 8 4 Z" Fill="{DynamicResource TextColor}"/>
                        </Grid>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#10000000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    
                    <!-- Header context menu -->
                    <Grid.ContextMenu>
                        <ContextMenu x:Name="HeaderContextMenu">
                            <MenuItem Header="Auto-size Column" Click="AutoSizeColumn_Click">
                                <MenuItem.Icon>
                                    <Path Width="12" Height="12" 
                                          Data="M 2 0 L 2 12 M 10 0 L 10 12 M 5 3 L 7 6 L 5 9"
                                          Stroke="{DynamicResource TextColor}" StrokeThickness="1"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Auto-size All Columns" Click="AutoSizeAllColumns_Click">
                                <MenuItem.Icon>
                                    <Path Width="12" Height="12" 
                                          Data="M 0 0 L 0 12 M 4 0 L 4 12 M 8 0 L 8 12 M 12 0 L 12 12"
                                          Stroke="{DynamicResource TextColor}" StrokeThickness="1"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Reset Column Widths" Click="ResetColumnWidths_Click">
                                <MenuItem.Icon>
                                    <Path Width="12" Height="12" 
                                          Data="M 2 6 A 4 4 0 1 1 6 2 L 6 4 L 8 2 L 6 0 L 6 2"
                                          Stroke="{DynamicResource TextColor}" StrokeThickness="1.5"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </Grid.ContextMenu>
                </Grid>
            </ScrollViewer>
        </Border>
        
        <!-- ScrollViewer to sync horizontal scrolling between header and content -->
        <ScrollViewer Grid.Row="1" x:Name="TreeScrollViewer" 
                      HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto"
                      ScrollChanged="TreeScrollViewer_ScrollChanged"
                      CanContentScroll="True"
                      VirtualizingStackPanel.ScrollUnit="Pixel">
            <!-- File TreeView with custom virtualizing panel -->
            <TreeView x:Name="fileTreeView" BorderThickness="0"
                     Background="{DynamicResource TreeViewBackground}"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     VirtualizingStackPanel.ScrollUnit="Pixel"
                     VirtualizingStackPanel.CacheLengthUnit="Page"
                     VirtualizingStackPanel.CacheLength="2,2"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                     ScrollViewer.IsDeferredScrollingEnabled="False"
                     Tag="{Binding IsMultiSelectMode, Converter={StaticResource BoolToMultiSelectTagConverter}}"
                     ItemsSource="{Binding RootItems}">
                
                <!-- Optimized Item Container Style -->
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource TreeViewItemWithLines}">
                        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
                        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                
                <!-- Use custom virtualizing panel for better column management -->
                <TreeView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <local:FileTreeVirtualizingPanel x:Name="TreeItemsPanel" ColumnSpacing="5"/>
                    </ItemsPanelTemplate>
                </TreeView.ItemsPanel>
                
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <!-- Grid WITHOUT shared size groups for better performance -->
                        <Grid x:Name="ItemGrid">
                            <Grid.ColumnDefinitions>
                                <!-- Checkbox column for multi-selection -->
                                <ColumnDefinition Width="Auto"/>
                                <!-- Fixed column widths - synchronization handled by custom panel -->
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Multi-selection checkbox -->
                            <CheckBox Grid.Column="0" 
                                      Margin="4,0,4,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Visibility="{Binding RelativeSource={RelativeSource AncestorType=TreeView}, 
                                                  Path=Tag, 
                                                  Converter={StaticResource MultiSelectVisibilityConverter}}"
                                      Click="SelectionCheckBox_Click"/>
                            
                            <!-- Name Column with tree structure -->
                            <Grid Grid.Column="1" ClipToBounds="True">
                                <!-- Background for selection -->
                                <Rectangle x:Name="SelectionBackground" Fill="Transparent" RadiusX="2" RadiusY="2"/>
                                
                                <!-- Tree structure content -->
                                <Grid>
                                    <!-- Background canvas for drawing connecting lines -->
                                    <Canvas Background="Transparent"
                                           Margin="{Binding Level, Converter={StaticResource LevelToIndentConverter}}"
                                           Height="20" HorizontalAlignment="Left" Width="40"
                                           ClipToBounds="False">
                                        
                                        <!-- Horizontal line to expander -->
                                        <Line x:Name="HorizontalLine"
                                              X1="0" Y1="10" X2="25" Y2="10"
                                              Stroke="{DynamicResource TreeLineColor}"
                                              StrokeThickness="1">
                                            <Line.Style>
                                                <Style TargetType="Line">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Line.Style>
                                        </Line>
                                        
                                        <!-- Vertical line from parent -->
                                        <Line x:Name="VerticalLine"
                                              X1="0" Y1="0" X2="0" Y2="10"
                                              Stroke="{DynamicResource TreeLineColor}"
                                              StrokeThickness="1">
                                            <Line.Style>
                                                <Style TargetType="Line">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Line.Style>
                                        </Line>
                                    </Canvas>
                                    
                                    <!-- Name content with indentation and expander -->
                                    <StackPanel Orientation="Horizontal"
                                               Margin="{Binding Level, Converter={StaticResource ExpanderRightIndentConverter}}">
                                        
                                        <!-- Expander Toggle Button -->
                                        <ToggleButton IsChecked="{Binding IsExpanded, Mode=TwoWay}"
                                                     VerticalAlignment="Center">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton" BasedOn="{StaticResource Windows11ExpanderStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDirectory}" Value="False">
                                                            <Setter Property="Visibility" Value="Hidden" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasChildren}" Value="False">
                                                            <Setter Property="Visibility" Value="Hidden" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>
                                        
                                        <!-- File/Folder Icon -->
                                        <Image Source="{Binding Icon}" Width="16" Height="16" 
                                               Margin="2,0,5,0" VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                        
                                        <!-- File/Folder Name -->
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"
                                                   Foreground="{Binding Foreground}"
                                                   FontWeight="{Binding FontWeight}"
                                                   ToolTip="{Binding Path}"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                            
                            <!-- Size Column -->
                            <TextBlock Grid.Column="3" Text="{Binding Size}" 
                                      VerticalAlignment="Center" 
                                      TextAlignment="Right"
                                      Margin="5,0"
                                      TextTrimming="CharacterEllipsis"
                                      Foreground="{Binding Foreground}"
                                      FontWeight="{Binding FontWeight}"/>
                            
                            <!-- Type Column -->
                            <TextBlock Grid.Column="5" Text="{Binding Type}" 
                                      VerticalAlignment="Center" 
                                      TextAlignment="Left"
                                      Margin="5,0"
                                      TextTrimming="CharacterEllipsis"
                                      Foreground="{Binding Foreground}"
                                      FontWeight="{Binding FontWeight}"/>
                            
                            <!-- Date Modified Column -->
                            <TextBlock Grid.Column="7" Text="{Binding LastModifiedStr}" 
                                      VerticalAlignment="Center" 
                                      TextAlignment="Left"
                                      Margin="5,0"
                                      TextTrimming="CharacterEllipsis"
                                      Foreground="{Binding Foreground}"
                                      FontWeight="{Binding FontWeight}"/>
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </ScrollViewer>
    </Grid>
</UserControl>