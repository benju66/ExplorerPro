
<UserControl x:Class="ExplorerPro.UI.FileTree.ImprovedFileTreeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExplorerPro.UI.FileTree"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Define GridView here to use as a resource -->
        <GridView x:Key="columnHeaderGridView" x:Name="columnHeaderGridView">
            <!-- Columns will be added programmatically -->
        </GridView>
    </UserControl.Resources>
    
    <Grid>
        <!-- Column Headers Row -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Column Headers with Border for background -->
        <Border Grid.Row="0" Background="#F0F0F0">
            <GridViewHeaderRowPresenter x:Name="headerPresenter" 
                                       Columns="{Binding Source={StaticResource columnHeaderGridView}, Path=Columns}"/>
        </Border>
        
        <!-- File TreeView -->
        <TreeView x:Name="fileTreeView" Grid.Row="1" BorderThickness="0"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    <Setter Property="FontWeight" Value="{Binding FontWeight}"/>
                    <Setter Property="Foreground" Value="{Binding Foreground}"/>
                </Style>
            </TreeView.ItemContainerStyle>
            
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <!-- Columns must align with header columns -->
                            <ColumnDefinition Width="250"/> <!-- Name -->
                            <ColumnDefinition Width="100"/> <!-- Size -->
                            <ColumnDefinition Width="120"/> <!-- Type -->
                            <ColumnDefinition Width="150"/> <!-- Modified Date -->
                        </Grid.ColumnDefinitions>
                        
                        <!-- Name Column -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Size Column -->
                        <TextBlock Grid.Column="1" Text="{Binding Size}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        
                        <!-- Type Column -->
                        <TextBlock Grid.Column="2" Text="{Binding Type}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        
                        <!-- Date Modified Column -->
                        <TextBlock Grid.Column="3" Text="{Binding LastModifiedStr}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>