<UserControl x:Class="ExplorerPro.UI.MainWindow.MainWindowTabs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExplorerPro.UI.MainWindow"
             xmlns:converters="clr-namespace:ExplorerPro.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Value converters for hiding close button on single tab -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        
        <!-- Converter that returns Visibility.Collapsed when count is 1, Visible otherwise -->
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
        
        <!-- Converter that returns false when count is 1, true otherwise -->
        <converters:CountToEnableConverter x:Key="CountToEnableConverter" />

        <!-- Animation Storyboards -->
        <Storyboard x:Key="TabHoverStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                           To="1.05" Duration="0:0:0.15" />
            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                          To="#E8F4F8" Duration="0:0:0.15" />
        </Storyboard>
        
        <Storyboard x:Key="TabHoverExitStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                           To="1.0" Duration="0:0:0.15" />
            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                          To="Transparent" Duration="0:0:0.15" />
        </Storyboard>

        <Storyboard x:Key="TabFadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           From="0" To="1" Duration="0:0:0.3" />
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                           From="30" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="TabHibernateFadeStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           To="0.6" Duration="0:0:0.4" />
        </Storyboard>

        <Storyboard x:Key="TabRestoreStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           To="1.0" Duration="0:0:0.3" />
        </Storyboard>
    </UserControl.Resources>
    
    <Grid>
        <TabControl x:Name="TabControl" 
                  TabStripPlacement="Top"
                  BorderThickness="0"
                  Padding="0"
                  Background="Transparent"
                  AllowDrop="True"
                  SelectionChanged="TabControl_SelectionChanged">
            <TabControl.Resources>
                <!-- Modern Chrome-style TabItem -->
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabItem}">
                                <Border x:Name="TabBorder"
                                      Background="Transparent"
                                      BorderBrush="#CCCCCC"
                                      BorderThickness="1,1,1,0"
                                      CornerRadius="8,8,0,0"
                                      Margin="0,2,2,0"
                                      Padding="0"
                                      RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform/>
                                            <TranslateTransform/>
                                        </TransformGroup>
                                    </Border.RenderTransform>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="8"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Hibernation indicator -->
                                        <Ellipse x:Name="HibernationIndicator" 
                                               Grid.Column="0"
                                               Width="4" Height="4"
                                               Fill="#FFA500"
                                               Margin="2,0,0,0"
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                        
                                        <!-- Tab title -->
                                        <TextBlock x:Name="TabTitle"
                                                 Grid.Column="1" 
                                                 Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}" 
                                                 Margin="8,8,4,8"
                                                 VerticalAlignment="Center"
                                                 FontSize="13"
                                                 FontWeight="Normal"
                                                 Foreground="#333333"
                                                 TextTrimming="CharacterEllipsis"/>
                                        
                                        <!-- Close button -->
                                        <Button x:Name="CloseButton"
                                              Grid.Column="2" 
                                              Content="×" 
                                              FontSize="14"
                                              FontWeight="Bold"
                                              Width="20" Height="20"
                                              Margin="4,0,4,0"
                                              BorderThickness="0"
                                              Background="Transparent"
                                              Foreground="#666666"
                                              VerticalAlignment="Center"
                                              Click="TabCloseButton_Click"
                                              ToolTip="Close Tab"
                                              Visibility="{Binding ElementName=TabControl, Path=Items.Count, 
                                                         Converter={StaticResource CountToVisibilityConverter}}"
                                              Style="{x:Null}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="CloseButtonBorder"
                                                          Background="Transparent"
                                                          CornerRadius="10"
                                                          Padding="2">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="CloseButtonBorder" Property="Background" Value="#E81123"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>
                                
                                <ControlTemplate.Triggers>
                                    <!-- Selected tab -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="Background" Value="White"/>
                                        <Setter TargetName="TabBorder" Property="BorderBrush" Value="#0078D4"/>
                                        <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,2,1,0"/>
                                        <Setter TargetName="TabTitle" Property="Foreground" Value="#0078D4"/>
                                        <Setter TargetName="TabTitle" Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Panel.ZIndex" Value="1"/>
                                    </Trigger>
                                    
                                    <!-- Hover effect -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource TabHoverStoryboard}"/>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard Storyboard="{StaticResource TabHoverExitStoryboard}"/>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                    
                                    <!-- Hibernated tab -->
                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Hibernated">
                                        <Setter TargetName="HibernationIndicator" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="TabTitle" Property="Foreground" Value="#999999"/>
                                        <Setter TargetName="TabTitle" Property="FontStyle" Value="Italic"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="MinWidth" Value="120"/>
                    <Setter Property="MaxWidth" Value="240"/>
                    <Setter Property="Height" Value="32"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TransformGroup>
                                <ScaleTransform/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Tab Control Header (Right-Side Controls) -->
            <TabControl.Template>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Tab Header with Extra Buttons -->
                        <Grid Grid.Row="0" Background="#F3F3F3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Tab Headers -->
                            <TabPanel x:Name="HeaderPanel" 
                                     Grid.Column="0"
                                     Panel.ZIndex="1" 
                                     IsItemsHost="True" 
                                     KeyboardNavigation.TabIndex="1"
                                     Background="Transparent" />

                            <!-- Right-side Buttons -->
                            <StackPanel Grid.Column="1" 
                                       Orientation="Horizontal" 
                                       VerticalAlignment="Center" 
                                       Margin="5,2">
                                <!-- Add Tab Button -->
                                <Button x:Name="AddTabButton" 
                                       Content="+" 
                                       ToolTip="Open a new tab"
                                       Click="AddTabButton_Click"
                                       Width="28" 
                                       Height="28" 
                                       Margin="3,0"
                                       BorderThickness="1"
                                       BorderBrush="#CCCCCC"
                                       Background="White"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="#666666">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4F8"/>
                                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Toggle Pinned Panel Button -->
                                <Button x:Name="TogglePinnedButton" 
                                       ToolTip="Toggle Pinned Panel"
                                       Click="TogglePinnedPanel_Click"
                                       Width="28" 
                                       Height="28" 
                                       Margin="3,0"
                                       BorderThickness="1"
                                       BorderBrush="#CCCCCC"
                                       Background="White">
                                    <Image Source="/Assets/Icons/pin.svg" Width="16" Height="16"/>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4F8"/>
                                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Toggle Bookmarks Button -->
                                <Button x:Name="ToggleBookmarksButton" 
                                       ToolTip="Toggle Bookmarks Panel"
                                       Click="ToggleBookmarksPanel_Click"
                                       Width="28" 
                                       Height="28" 
                                       Margin="3,0"
                                       BorderThickness="1"
                                       BorderBrush="#CCCCCC"
                                       Background="White">
                                    <Image Source="/Assets/Icons/star.svg" Width="16" Height="16"/>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4F8"/>
                                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Toggle Procore Links Button -->
                                <Button x:Name="ToggleProcoreButton" 
                                       ToolTip="Toggle Procore Links Panel"
                                       Click="ToggleProcorePanel_Click"
                                       Width="28" 
                                       Height="28" 
                                       Margin="3,0"
                                       BorderThickness="1"
                                       BorderBrush="#CCCCCC"
                                       Background="White">
                                    <Image Source="/Assets/Icons/link.svg" Width="16" Height="16"/>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4F8"/>
                                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Toggle To-Do Panel Button -->
                                <Button x:Name="ToggleTodoButton" 
                                       ToolTip="Toggle To-Do Panel"
                                       Click="ToggleTodoPanel_Click"
                                       Width="28" 
                                       Height="28" 
                                       Margin="3,0"
                                       BorderThickness="1"
                                       BorderBrush="#CCCCCC"
                                       Background="White">
                                    <Image Source="/Assets/Icons/list-todo.svg" Width="16" Height="16"/>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4F8"/>
                                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!-- Tab Content -->
                        <Border Grid.Row="1" 
                               BorderThickness="0,1,0,0" 
                               BorderBrush="#CCCCCC" 
                               Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="PART_SelectedContentHost" 
                                            ContentSource="SelectedContent" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
            
            <!-- Context Menu for TabControl -->
            <TabControl.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="New Tab" Click="NewTabMenuItem_Click" />
                    <MenuItem Header="Duplicate Tab" Click="DuplicateTabMenuItem_Click" />
                    <MenuItem Header="Close Tab" Click="CloseTabMenuItem_Click" 
                             IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=TabControl}, 
                                        Path=Items.Count, Converter={StaticResource CountToEnableConverter}}" />
                    <Separator />
                    <MenuItem Header="Hibernate Tab" Click="HibernateTabMenuItem_Click" 
                             IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=TabControl}, 
                                        Path=Items.Count, Converter={StaticResource CountToEnableConverter}}" />
                    <MenuItem Header="Restore Tab" Click="RestoreTabMenuItem_Click" />
                    <Separator />
                    <MenuItem Header="Toggle Split View" Click="ToggleSplitViewMenuItem_Click" />
                    <MenuItem Header="Detach Tab" Click="DetachTabMenuItem_Click" 
                             IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=TabControl}, 
                                        Path=Items.Count, Converter={StaticResource CountToEnableConverter}}" />
                    <MenuItem Header="Move to New Window" Click="MoveToNewWindowMenuItem_Click" 
                             IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=TabControl}, 
                                        Path=Items.Count, Converter={StaticResource CountToEnableConverter}}" />
                </ContextMenu>
            </TabControl.ContextMenu>
        </TabControl>
    </Grid>
</UserControl>